import React from 'react';
import CatBanner from '../../Molecules/Banners/CatBanner';

const AppDevCtaBanner: React.FC = () => {
	return (
		<CatBanner
			title="Ready to discuss your project?"
			text="We’d love to connect and discuss how we can help you bridge the gap between an idea and a top class product."
			buttonText="Book free consultation"
		/>

	);
};

export default AppDevCtaBanner;
